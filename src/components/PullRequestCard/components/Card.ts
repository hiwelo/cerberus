import styled from 'styled-components';

export default styled.article`
  display: grid;
  grid-template-areas:
    'cardTitle cardTitle'
    'cardInfo cardDeployments';
  grid-column-gap: 0;
  grid-row-gap: 0;
`;
