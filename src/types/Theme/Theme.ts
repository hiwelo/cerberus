import { ThemePalette } from './ThemePalette';

/** Describes the Theme to use to style this application */
export interface Theme {
  /** Color palette to use to style the application */
  palette: ThemePalette;
}
